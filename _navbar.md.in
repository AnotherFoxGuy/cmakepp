<%
# this reads all files cmake
assign(readmes = glob("cmake/*/README.md" --relative))
string(REPLACE "//" "/" readmes "${readmes}")
%>
<% foreach(readme ${readmes})%>- <%=markdown_template_link('${readme}')%>
<% endforeach() %>